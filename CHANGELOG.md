# Changelog

## v2.4.0

- Update upstream Sarasa Term SC to `1.0.37`.
- Update Nerd Fonts to `3.4.0` and Font Patcher to `4.20.3`.
- Add `scripts/refresh-upstream.sh` to refresh upstream assets and regenerate `scripts/font-patcher`.
- Add `scripts/build-release-assets.sh` to build hinted + unhinted release artifacts into `dist/`.
- Add `.gitignore` and ignore repo-local `tmp/` workspace.

